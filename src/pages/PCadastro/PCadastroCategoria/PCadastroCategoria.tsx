import FormCategoria from "../../../components/Formularios/FormCategoria/FormCategoria";

function PCadastroCategoria() {
    return (
        <main>
            <FormCategoria />
        </main>
    );
}

export default PCadastroCategoria;